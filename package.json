{
  "name": "css-acp-repro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve:wac": "rm -rf ./ownerName && rm -rf ./.internal && community-solid-server",
    "serve:acp": "rm -rf ./ownerName && rm -rf ./.internal && community-solid-server -c ./.css.config.json",
    "serve:acp:memory": "rm -rf ./ownerName && rm -rf ./.internal && community-solid-server -c ./.css.config.memory.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jeswr/css-auth-utils": "^1.3.0",
    "@solid/community-server": "^6.0.0",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
